package debug;

/**
 * This class servers mainly as static variables which says if we are in debug mode or not.
 * Basically as #define in c/c++.
 */
public class DEBUG_CLASS {
    public static final boolean DEBUG = false;
}